<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}TicketFlow{% endblock %}</title>
    {# Favicon (vous pouvez garder le vôtre ou le personnaliser) #}
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>TF</text></svg>">

    {# Lien vers votre fichier CSS principal qui contiendra les styles #}
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    {# Si vous créez un fichier dédié theme.css ou glpi_inspired.css #}
    {# <link rel="stylesheet" href="{{ asset('styles/theme.css') }}"> #}

    {% block stylesheets %}{% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body>
    <div class="app-container"> {# Conteneur global pour layout flex si sidebar #}
        <header class="app-header">
            <div class="logo">
                <a href="{{ path('homepage') }}">TicketFlow</a>
            </div>
            <nav class="main-navigation">
                <a href="{{ path('homepage') }}">Accueil</a>
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('user_index') }}">Gérer les utilisateurs</a>
                {% endif %}
                {# Ajoutez d'autres liens de navigation principaux ici #}
            </nav>
            <div class="user-info">
                {% if app.user %}
                    <span>Connecté : <strong>{{ app.user.email }}</strong></span>
                    <a href="{{ path('app_logout') }}" class="btn btn-logout">Déconnexion</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-login">Connexion</a>
                {% endif %}
            </div>
        </header>

        {# Optionnel: Si vous voulez un layout avec sidebar comme GLPi #}
        {# <div class="app-body"> #}
            {# <aside class="app-sidebar"> #}
                {# {% block sidebar_nav %} #}
                    {# La navigation principale pourrait être ici si vous la déplacez #}
                {# {% endblock %} #}
            {# </aside> #}

            <main class="app-main-content">
                 {% for label, messages in app.flashes %}
                    <div class="flash-messages">
                        {% for message in messages %}
                            <div class="alert alert-{{ label }}">
                                {{ message }}
                            </div>
                        {% endfor %}
                    </div>
                {% endfor %}

                <div class="content-wrapper"> {# Remplace .container-main pour la sémantique #}
                    {% block body %}{% endblock %}
                </div>
            </main>
        {# </div> .app-body #}

        {% block footer %}
            <footer class="app-footer">
                <p>&copy; {{ "now"|date("Y") }} TicketFlow. Tous droits réservés.</p>
            </footer>
        {% endblock %}
    </div>
</body>
</html>