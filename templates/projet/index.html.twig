{% extends 'base.html.twig' %}

{% block title %}Liste des projets{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card p-4 shadow rounded">
        <h1 class="mb-4 text-center">📋 Liste des projets</h1>

        <div class="mb-3 text-end">
            <a href="{{ path('app_projet_new') }}" class="btn btn-success">➕ Créer un nouveau projet</a>
        </div>

        <div class="table-responsive">
            <table class="table table-bordered table-hover align-middle text-center">
                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Référence</th>
                        <th>Nom</th>
                        <th>Description</th>
                        <th>Date début</th>
                        <th>Date fin</th>
                        <th>Statut</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for projet in projets %}
                        <tr>
                            <td>{{ projet.id }}</td>
                            <td>{{ projet.projetId }}</td> {# correction ici #}
                            <td>{{ projet.nomProjet }}</td>
                            <td>{{ projet.descriptionProjet }}</td>
                            <td>{{ projet.dateDebut ? projet.dateDebut|date('d/m/Y H:i') : '—' }}</td>
                            <td>{{ projet.dateFin ? projet.dateFin|date('d/m/Y H:i') : '—' }}</td>
                            <td>{{ projet.satut }}</td>
                            <td>
                                <a href="{{ path('app_projet_show', {'id': projet.id}) }}" class="btn btn-outline-primary btn-sm">👁 Voir</a>
                                <a href="{{ path('app_projet_edit', {'id': projet.id}) }}" class="btn btn-outline-warning btn-sm">✏ Modifier</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="8" class="text-center text-muted">Aucun projet trouvé.</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
